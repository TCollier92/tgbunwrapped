# ----------------------------------------------------------------------
# Node.js/Express specific ignores
# ----------------------------------------------------------------------

# Dependencies: The 'node_modules' folder is built by 'npm install'
# and should never be committed, especially since it's built inside the container.
node_modules
# Optional: yarn/pnpm lock files if you decide to switch package managers
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Runtime data
.eslintcache
.prettiercache

# ----------------------------------------------------------------------
# VS Code / Dev Container ignores
# ----------------------------------------------------------------------

# The Dev Container setup creates temporary environment files.
.devcontainer/.devcontainer.state.json

# VS Code's local settings folder (not used when using devcontainer.json)
.vscode/settings.json
.vscode/tasks.json
.vscode/launch.json # <-- NOTE: If you only want to share launch.json with the container, you can remove this line.
.vscode/*.log

# ----------------------------------------------------------------------
# Operating System / IDE ignores
# ----------------------------------------------------------------------

# macOS
.DS_Store

# Windows
Thumbs.db

# JetBrains IDEs (like WebStorm)
.idea